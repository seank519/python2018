# CBT 연습 테스트 실행 방법

1. 이 폴더에서 간단한 서버를 띄웁니다.
   ```bash
   python -m http.server 8000
   ```
2. 브라우저로 `http://localhost:8000/index.html` 에 접속합니다.

> 단순히 `index.html` 파일을 더블 클릭해도 열리지만, 위처럼 서버를 띄우면 버튼 동작이 더 확실합니다.
